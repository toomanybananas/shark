#pragma once
#include <iostream>
#include <string>
#include <sstream>
#include <fstream>
#include <map>
#include <algorithm>

std::map<std::string, std::string> LoadInfo(std::string path, int& result);
